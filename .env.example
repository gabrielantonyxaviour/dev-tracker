# Server port (default: 3000)
PORT=3020

# SQLite database path (default: ./data/dev-tracker.db)
DB_PATH=./data/dev-tracker.db

# Path to Claude Code projects directory (default: ~/.claude/projects)
# This is where Claude Code stores session JSONL files
CLAUDE_PROJECTS_DIR=/path/to/your/.claude/projects
